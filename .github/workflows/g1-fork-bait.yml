name: g1-fork-bait
on:
  pull_request: {}
jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: upload crafted artifact
        uses: ./.github/actions/evil-upload
      - name: runtime surface probe
        uses: ./.github/actions/probe-runtime
      - name: artifact name escape upload
        uses: ./.github/actions/name-escape
